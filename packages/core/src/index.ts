/**
 * @topicmd/core — placeholder entry.
 *
 * Real exports land in later tasks: type definitions (#2), schema loader (#4),
 * topic parser (#5), fragment resolver (#6), validator (#7), indexer (#8), etc.
 */
export const VERSION = '0.0.0';
