---
topic_type: concept
dimensions:
  product: alpha
---

# Missing required fields

A `concept` requires `title` and `description`; both are absent here. Expected
diagnostics: `missing-required-field` for `title` and `description`.
