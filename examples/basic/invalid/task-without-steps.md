---
title: "Task without steps"
topic_type: task
dimensions:
  audience: admin
---

# Task without steps

This `task` has neither an `## Steps` heading nor an ordered list, violating the
task contract (`must_contain: steps`). Expected diagnostic: `contract-violation`.
