---
title: "Reference without version"
topic_type: reference
dimensions:
  product: beta
---

# Reference without version

A `reference` requires `version`, which is absent. Expected diagnostic:
`missing-required-field` for `version`.
